# Member repos cloned by bootstrap.sh — each is its own git repo, not tracked here.
/app/
/shared/
/hub/
/server/
/plugins/

# generated per-machine by bootstrap.sh (lists only the repos you cloned)
/myra.code-workspace

# junk
.DS_Store
*.bun-build
